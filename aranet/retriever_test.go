package aranet

/*
import (
	"os"
	"testing"
)

var aranetID = "to be set by TEST_ARANET"

func init() {
	if env, ok := os.LookupEnv("TEST_ARANET"); ok {
		aranetID = env
	} else {
		panic("tests require aranet and bluetooth id provided in TEST_ARANET ENV")
	}
}

func TestRetriever(t *testing.T) {
	r := Retriever{ID: aranetID}
	err := r.Update()
	if err != nil {
		t.Fatalf("expected update not to fail: %v", err)
	}
}
*/
